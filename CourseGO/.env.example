# Dev : IP LAN (même Wi‑Fi) ou URL tunnel (autre réseau / build installé).
# Tunnel actuel : npm run tunnel:api → coller l’URL https ici + dans eas.json env.
# Sur téléphone sans tunnel : http://<IP-WiFi-du-PC>:8787 (pas 127.0.0.1)
EXPO_PUBLIC_API_URL=http://172.20.10.2:8787
