# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

App staff du monorepo SuperU. API unique : `../server` (port 8787). Catalogue images : `../marche-dore/assets/images/catalog`.

Logins : client boutique ≠ staff CourseGO (`picker@` / `courier@`) ≠ back-office (`admin@` / `rh@`).
