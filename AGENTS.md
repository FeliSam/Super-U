# SuperU

Monorepo : `marche-dore` (client), `CourseGO` (staff terrain), `marche-admin` (web catalogue + RH, port 8083), `server` (API + Postgres).

Expo **v57** : https://docs.expo.dev/versions/v57.0.0/

Ne pas recréer une 2e base. Ne pas mélanger login client (`public.users`), login CourseGO et login admin/RH (`ops.staff`).
